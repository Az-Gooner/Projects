import numbers


numbers = [1, 2, 3, 4, 5]
numbers.append(6) #adds a number to the end of the list 
print(numbers)


# number.insert(0, -1) adds an item to a list depending on the location that you first stated. Here the code will attach -1 at the first position of the list 
# number.remove() removes a number from the list 
# number.clear() will remove every number from the list 